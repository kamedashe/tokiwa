"use client";

/**
 * Свой лоадер вместо оптимизатора Vercel: на бесплатном тарифе квота
 * трансформаций сгорела за два дня (15 тыс. уникальных постеров), и всё,
 * что не успело в кэш, стало отдавать 402 — «некоторые картинки не грузятся».
 *
 * Отдаём адрес как есть, без подбора размеров. У Shikimori «оригинал»
 * постера — всего 225×318 и ~45 КБ, так что экономить на нём нечем:
 * подмена на /preview/ (160×226) делала мыльными даже карточки.
 * AniList и прочие хосты тоже хранят постеры разумного размера.
 */
export default function imageLoader({ src }: { src: string; width: number }) {
  return src;
}
